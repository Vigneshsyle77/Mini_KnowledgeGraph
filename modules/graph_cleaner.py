 
Instructions:

Students must:
Create a Dockerfile inside their project.
Add all essential steps (base image → dependencies → run app).
Build their Docker image.
Run the project inside a container.
Take screenshots of:
Docker build success
Docker container running
AI-KnowMap UI running on browser from the container
Write a short reflection:
What worked easily?
What errors happened?
Why is Docker important for this project?